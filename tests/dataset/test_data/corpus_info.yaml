test_corpus:
  description: Test dataset
  annotations:
    - annot1
  partitions:
    - speaker
    - label
  subsets:
    default: all
    all:
      clips: all_clips
      description: All files
    all_unsorted:
      clips: all_clips_unsorted
      description: All files, but unsorted, for testing purposes.
    subset1:
      clips: subset
  features_dir: features
