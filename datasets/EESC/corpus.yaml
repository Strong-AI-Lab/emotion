EESC:
  description: Estonian emotional speech corpus
  annotations:
  partitions:
    - label
    - language
    - country
    - transcript
  subsets:
    default: all
    all:
      clips: files_all
      description: All files
  features_dir: ../../features/EMO-DB
