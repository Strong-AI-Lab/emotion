URDU:
  description: URDU dataset
  annotations:
  partitions:
    - label
    - speaker
    - language
  subsets:
    default: all
    all:
      clips: files
      description: All files
  features_dir: ../../features/URDU
