CaFE:
  description: CaFE dataset
  annotations:
  partitions:
    - label
    - language
    - speaker
    - gender
  subsets:
    default: all
    all:
      clips: files_all
      description: All files
  features_dir: ../../features/CaFE
